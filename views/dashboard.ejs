<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard do Usuário</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
    integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

  <style>
    body {
      background: url('/public/bgEnergIA3.jpg') no-repeat center center fixed;
      background-size: cover;
      color: #fff;
      font-family: Arial, sans-serif;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      /* Alinhamento no topo para deixar espaço para crescer */
      margin: 0;
      padding: 20px 0;
    }

    .dashboard-container {
      width: 95%;
      max-width: 1200px;
      padding: 20px;
      border: 3px solid #fff;
      border-radius: 15px;
      background-color: rgba(0, 0, 0, 0.0);
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .logo-small {
      left: 10px;
      width: 150px;
    }





    .greeting {
      font-size: 1em;
      font-weight: bold;
    }



    .icon-user {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background-color: #666;
    }

    .icon-user i {
      color: #fff;
      /* Cor do ícone */
      font-size: 1.5rem;
      /* Tamanho do ícone */
    }

    .footer {
      text-align: center;
      padding: 10px 0;
      color: #fff;
    }

    .footer p {
      margin-bottom: 0;
    }

    @media (max-width: 768px) {
      .dashboard-container {
        margin: 20px;
      }

      .btn-custom {
        font-size: 1em;
        padding: 10px;
      }

      .greeting {
        font-size: 0.9em;
      }

      .icon-container {
        padding: 8px;
      }

      .icon-user {
        width: 40px;
        height: 40px;
      }

      /* Ajuste para manter o logo e o ícone do usuário na mesma linha em mobile */
      .row.align-items-center {
        display: flex;
        align-items: center;
        justify-content: space-between;
        /* Garante que o logo e o ícone fiquem nas extremidades */
      }

      .logo-small {
        width: 100px;
        /* Reduzir o tamanho do logo em dispositivos menores */
      }

      .icon-user {
        width: 40px;
        height: 40px;
      }

      .btn-conversar {
        height: 25vh;
        /* Aumentar a altura em resoluções menores para caber os elementos */
      }

      .btn-conversar span {
        font-size: 1.8rem;
        /* Ajuste o tamanho da fonte em telas menores */
      }

      .btn-conversar .icon-lamp {
        font-size: 2rem;
        /* Ajuste o tamanho do ícone em telas menores */
      }

    }

    .user-dropdown-menu {
      position: absolute;
      top: 60px;
      right: 0;
      background: #222;
      border: 1px solid #fff;
      border-radius: 8px;
      min-width: 120px;
      z-index: 100;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
    }

    .user-dropdown-menu .dropdown-item {
      color: #fff;
      padding: 10px 18px;
      display: block;
      text-decoration: none;
      border-bottom: 1px solid #444;
      transition: background 0.2s;
    }

    .user-dropdown-menu .dropdown-item:last-child {
      border-bottom: none;
    }

    .user-dropdown-menu .dropdown-item:hover {
      background: #444;
    }

    .btn-conversar-large {
      background: linear-gradient(135deg, #009D17 0%, #01EA41 100%);
      color: #fff;
      border: none;
      border-radius: 50px;
      padding: 15px 40px;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(0, 255, 65, 0.4);
      max-width: 400px;
      position: relative;
      overflow: hidden;
    }

    .btn-conversar-large:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 25px rgba(0, 255, 65, 0.6);
      columns: #fff;
      text-decoration: none;
    }

    .btn-conversar-large:hover i {
      transform: scale(1.1);
    }
  </style>
  <div id="user-metadata" style="display:none;" data-userid="<%= userId %>" data-group="<%= group %>"></div>
</head>

<body>
  <div class="dashboard-container">
    <!-- Primeira Row: Saudação do usuário + Ícone -->
    <div class="row align-items-center mb-4">
      <div class="col-9 col-md-9">
        <img src="/public/logo-EnergIA.png" alt="Logo Energia" class="logo-small">
      </div>
      <div class="col-3 col-md-3 d-flex justify-content-end position-relative">
        <div class="icon-user d-flex align-items-center justify-content-center" id="userMenuBtn"
          style="cursor:pointer;">
          <i class="fas fa-user"></i>
          <span class="burger-lines ml-2 d-md-none">
            <i class="fas fa-bars"></i>
          </span>
        </div>
        <!-- Dropdown Menu -->
        <div id="userDropdown" class="user-dropdown-menu" style="display:none;">
          <% if (typeof role !=='undefined' && role==='godmode' ) { %>
            <a href="/admin" class="dropdown-item" style="color: #ef4444; font-weight: bold;">⚡ Godmode</a>
            <% } %>
              <a href="/logout" class="dropdown-item">Logoff</a>
        </div>
      </div>

    </div>

    <!-- Main Content: Centered Layout -->
    <div class="row justify-content-center align-items-center" style="min-height: 60vh;">
      <div class="col-md-8 text-center">

        <!-- Greeting Section -->
        <div class="mb-5">
          <h1 class="display-4 font-weight-bold">Olá, <%= userName %>!</h1>
          <p class="lead" style="color: #cbd5e1;">Pronto para otimizar seu consumo de energia hoje?</p>
        </div>

        <!-- Weather Widget (Simplified) -->
        <div class="d-flex justify-content-center mb-5">
          <div class="weather-pill d-flex align-items-center px-4 py-2"
            style="background: rgba(255,255,255,0.1); border-radius: 50px; backdrop-filter: blur(5px); border: 1px solid rgba(255,255,255,0.2);">
            <% if (weatherIcon) { %>
              <img src="https://openweathermap.org/img/wn/<%= weatherIcon %>@2x.png" alt="Weather"
                style="width: 50px; height: 50px;">
              <% } %>
                <span class="ml-3 h4 mb-0 font-weight-bold">
                  <%= temperature %>ºC
                </span>
                <span class="ml-2 text-muted">em Pelotas</span>
          </div>
        </div>

        <!-- Main Action Button -->
        <a href="/chat" class="btn btn-conversar-large mx-auto d-flex align-items-center justify-content-center">
          <div class="mr-3 p-3 rounded-circle" style="background: rgba(255,255,255,0.2);">
            <i class="fas fa-comments fa-2x"></i>
          </div>
          <div class="text-left">
            <div style="font-size: 0.9rem; text-transform: uppercase; letter-spacing: 1px;">Assistente Virtual</div>
            <div style="font-size: 1.5rem; font-weight: bold;">Iniciar Conversa</div>
          </div>
        </a>

      </div>
    </div>

    <!-- Rodapé com o nome do laboratório -->
    <footer class="footer">
      <p>LABCEE / UFPel - 2024 - 2026</p>
    </footer>
  </div>

  <script>
    const userMenuBtn = document.getElementById('userMenuBtn');
    const userDropdown = document.getElementById('userDropdown');
    document.addEventListener('click', function (event) {
      if (userMenuBtn.contains(event.target)) {
        userDropdown.style.display = userDropdown.style.display === 'block' ? 'none' : 'block';
      } else {
        userDropdown.style.display = 'none';
      }
    });
  </script>
</body>

</html>